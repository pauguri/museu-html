function accion1(s) {
    if (document.getElementById('s').style.transform == 'rotateZ(0deg)') {
        alert('.../12895487');
    }
}

var p;
function generar() {
    p = Math.floor(Math.random() * 99999);
    console.error('PASSWORD: ' + p);
}

function comprovar() {
    const i = document.getElementById('in');
    const n = document.getElementById('no');
    if (i.value == p) {
        window.location.href = window.location.href + '/76589085';
    } else {
        i.value = '';
        n.style.animation = 'opacityin 5s';
        n.onanimationend = () => {
            n.style.animation = 'none';
        };
    }
}

function chaos() {
    document.getElementById('stop').addEventListener('mouseenter', () => { document.body.style.backgroundColor = 'red' });
    document.getElementById('stop').addEventListener('mouseleave', () => { document.body.style.backgroundColor = 'black' });
}